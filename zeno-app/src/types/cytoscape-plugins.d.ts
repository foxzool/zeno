declare module 'cytoscape-cola' {
  import { ExtensionBuilder } from 'cytoscape';
  const extension: ExtensionBuilder;
  export default extension;
}

declare module 'cytoscape-fcose' {
  import { ExtensionBuilder } from 'cytoscape';
  const extension: ExtensionBuilder;
  export default extension;
}